from flask import Blueprint

auth = Blueprint('auth', __name__)

# NOTE:
# CloudCraft workshop does NOT require authentication.
# This file exists only to demonstrate project structure
# and how auth would be added in real-world applications.
